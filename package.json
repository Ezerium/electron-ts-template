{
  "name": "app-template",
  "version": "1.0.0",
  "description": "",
  "main": "dist/App.js",
  "scripts": {
    "build": "tsc && electron-packager . --platform=win32 --arch=x64 --out=build --overwrite",
    "start": "tsc && electron .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/electron": "^1.6.10",
    "electron": "^29.1.4",
    "electron-packager": "^17.1.2",
    "typescript": "^5.4.2"
  }
}
